import React from 'react';

const Navigation: React.FC = () => {
    return (
        <nav>
            <h1>Navigation</h1>
            {/* Add navigation links here */}
        </nav>
    );
};

export default Navigation;
